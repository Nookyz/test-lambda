import styled from 'styled-components'

export default styled.button`
  border: 0;
  border-radius: 3px;
  background-color: inherit;

  font-size: 16px;
  cursor: pointer;

  &:focus {
    outline: 0;
  }
`
